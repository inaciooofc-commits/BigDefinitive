'use strict';
// A implementação principal da integração Google Sheets fica em server.js para manter o pacote simples no antiX.
// Este arquivo documenta o serviço e serve como ponto de extração futura.
module.exports = {
  purpose: 'Google Sheets bootstrap/export/import for Big',
  sensitiveDataPolicy: 'Never export passwords, tokens, service-account keys or private message contents.'
};
